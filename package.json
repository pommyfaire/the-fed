{
   "scripts": {
      "start": "npm run prebuild && parcel src/index.html",
      "build": "parcel build src/index.html --public-url ./",
      "prebuild": "rimraf .parcel-cache dist",
      "postbuild": "node delete_maps"
   },
   "devDependencies": {
      "parcel": "^2.6.0",
      "process": "^0.11.10",
      "rimraf": "^3.0.2"
   },
   "dependencies": {
      "ethers": "^5.6.8"
   }
}
